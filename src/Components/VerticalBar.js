import React, {useState} from 'react';
import {StyleSheet, View, Text} from 'react-native';

// A simple component to display a vertical progress Bar
const VerticalBar = () => {
  const [data, changeData] = useState();

  return <View></View>;
};

export default VerticalBar;
